version: '3.8'
services:
  flatris:
    # Change from ${DOCKERHUB_USERNAME} to your actual username
    image: nazrulidb/flatris-app:latest
    container_name: flatris-production
    restart: always
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000

